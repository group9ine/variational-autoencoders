# potentials to choose from:
# LJ + polymer chain
# --> write "poly" followed by k,R0 values (comma in-between)
# LJ + gravity with gamma
# --> write "gamma" followed by gamma value
# p-LJ + gravity with gamma
# --> write "fake" followed by gamma value (comma in-between)
potential=gamma,0.4
n_parts=30
side=10
temp=0.75
max_disp=0.2
n_systems=2000
n_steps=2500
n_sample=1
print_en=false
show_z=false
